# AGENTS.md

## Release Request Rule

当用户提出“发布正式版本号”（例如 `0.1.0`、`0.1.1`、`1.0.0`）时，agent 在执行发版动作前必须先审查并确认 `README.md` 是否需要同步更新。

触发条件：

- 明确出现语义为“发正式版本”的请求
- 版本号是 semver 形式（`x.y.z`）
- 仅要求 commit、merge、push 不触发本规则

审查清单（至少逐项确认一次）：

1. 功能说明是否覆盖本次版本的新增/变更行为
2. `Current Version`（或等价版本章节）是否与目标版本一致
3. `Roadmap` 是否需要标记已实现项、下移未完成项或新增后续计划

执行要求：

- 若 README 需要更新，先完成 README 更新，再进行版本发布相关动作
- 若 README 无需更新，在回复中显式说明“已审查 README，无需变更”
